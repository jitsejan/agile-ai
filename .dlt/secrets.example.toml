[sources.jira]
# Copy this file to `.dlt/secrets.toml` and fill with your real values locally.
subdomain = "your-subdomain"
email = "your-email@example.com"
api_token = "REPLACE_WITH_REAL_API_TOKEN_OR_USE_ENV"
